global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: streampulse-api
    metrics_path: /metrics
    static_configs:
      - targets: ['api:8080']
        labels:
          service: streampulse-api
          env: dev
